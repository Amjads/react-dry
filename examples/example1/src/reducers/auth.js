import { actionTypes } from '../actions/auth';

const reducer = (state = {
// ##REDUCER_STATE##
}, action) => {
  switch (action.type) {
    // ##REDUCER_CASE##

    default:
      return state;
  }
};

export default reducer;
