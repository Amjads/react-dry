import { actionTypes } from '../actions/customers';

const reducer = (state = {

}, action) => {
  switch (action.type) {
    // ##REDUCER_CASE##

    default:
      return state;
  }
};

export default reducer;
